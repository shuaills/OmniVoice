
## B3（提案，未点火）：韵律对齐的动态块 —— cap + 早退边界 token

**动机**（诚实版）：非"修复块割裂"（B2 的 32 帧固定块在能量扫描与听感下无接缝伪影，
无病例可修），而是两个正向收益假说：① 流式输出按韵律短语吐块，节奏自然于 1.28s
机械滴答；② 韵律完整块可容忍更低的每块去噪步数 K → RTF 收益。
文本侧 reasoning-dLLM 的动态块动机（逻辑流割裂）在语音侧的对应物是韵律短语
（呼吸群/语调短语），标点≈其文字投影——语音版动机比文本版更物理。

**鸡生蛋约束**：块切在时间轴、标点在文本域；"逗号落在第几帧"本身是生成时决策。
故边界不可预排，必须是生成中模型自决的 token——与 EOS 同构、降一级作用域。

**机制**（EOS 机制的直接推广）：
- 词表 +1：[boundary]=1026（cb0 语义），vocab 1026→1027；
- 语义：韵律短语结束、块提前终结。块尺寸 = 模型自决 ∈ [1, 32]（32 仍为 cap，
  cache 页与训练几何的锚）；
- 训练标注：VAD/能量停顿检测原始 mp3（SER 侦察已证 865 tar 波形全可达），
  停顿帧为 [boundary] 候选；current-block 采样时块右缘对齐停顿候选；
  boundary-fill 语义与 eos-fill 同构（停顿区 cb0 标 [boundary]，其余码本不算 loss）；
- VAD 标注 = 廉价 CPU 活，挂 SER runner 同套基建顺路跑（增量 jsonl、done 标记、
  PAUSE 让路），不占卡；
- 推理：块内任一列出 [boundary] → 当前块就地截断提交、开新块；[eos] 语义不变
  （整句终结）。[boundary] 与 [eos] 同为稀有决策类，**第一天即 CFG 旁路**（E1 疤痕
  条款，不设消融后门）；
- cache：变长已提交块与页式 cache 无冲突（页分配与序列长度本就解耦）；
  块因果 mask 的"块 id"改由运行时边界表驱动，训练侧 mask_mod 增加边界表入参。

**排期条款**：B2 50k + 全量评测落袋（三条主 claim 钉死）之前不点火。点火时为
独立臂 shuai-block-b3，init 同一迁移 ckpt 或 B2 终点 ckpt（届时按单变量原则二选一）。

**报告叙事**：EOS（全局长度）/ 弹性画布（离线局部伸缩）/ 动态块边界（流式分段）
三机制一条 thesis：把时间轴的支配权还给模型。
