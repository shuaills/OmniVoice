# B2 技术报告草稿：从双向 Masked-Diffusion 到 Block-Causal dLLM 的低成本转换
（2026-07-07 初稿骨架，数字以 RESULTS.md 为准；[待填] = 实验在跑）

## 0. 一句话主张

一个训练在 58 万小时上的双向 masked-diffusion TTS（OmniVoice），只花 ~100 H100·时
（0.5 epoch，约为预训练成本的 1/1000）即可转换成 block-causal 的流式形态：
块间因果 + 块内并行去掩码 + 精确块级 KV cache + EOS 自决长度。
主表的残余差距不是"方法失败"，而是**流式的质量成本**的直接度量。

## 1. 动机与定位

- 双向画布质量高但天然不可流式：生成前须定长（时长预测器），全画布注意力无法增量。
- block dLLM（BD3-LM 系）给了中间形态：块内保留扩散并行性，块间恢复自回归因果性
  → 首块即可发声，KV cache 使增量代价与 AR 同阶。
- 与从头训练 block-diffusion TTS（Chatterbox-Flash 路线）不同，我们回答的问题是：
  **已有的双向权重能不能"廉价改装"成流式？** 答案：能，且改装成本 ~1/1000。

## 2. 方法

### 2.1 双拷贝转换训练（BD3-LM adapted）
- 序列布局：[text prefix | clean copy | noisy copy]，两拷贝共享 RoPE 位置。
- 每块独立 mask_ratio ~ U(0,1)；flex_attention 自定义 mask_mod 实现块因果
  （noisy 块看 text + clean 的严格前缀块 + 自身）。
- 训练-推理可见集恒等（gate 实证）；mask 承重（gate 实证：去掉 mask Δloss=0.342）。

### 2.2 精确块级 KV cache 解码
- cache 路径 ≡ 重算路径（fp32 逐位相同）。fp16 下 token 分叉=蝴蝶效应，
  质量对照（parity_cer）恒等 → 正确性标准应是质量 parity 而非 token 一致率。

### 2.3 EOS 自决长度（吸收态填充）
- cb0 词表 +[eos]=1025；EOS 后整块 padding。块最后统一 32 帧（1.28s @25fps）。
- 校准结果：停长/规则估计比 1.006；spearman(停长~文长)=0.79-0.81（55 文本集平台）。

### 2.4 决策 token 三重护栏（独立贡献，通用原则）
稀有决策 token（[eos]、弹性画布的 [expand]/[delete]）在 CFG 扩散解码器里会被
外推放大或过早触发。三重护栏，各管一层：
1. **CFG 旁路**（打分层）：决策 token 的 logit 不走 guidance 组合——E1 时代
   [expand] 风暴单因子即 CFG 外推（CER 0.958→0.006）。
2. **min_gen_frames 地板**（资格层）：低于 0.3×规则估计的帧位禁止 [eos]——
   治好 zh 首轮 30.6% 首块截断（616/618 死于首块中位第 5 帧）。
3. **max_blocks 兜底**（上界层）：EOS 永不触发时的硬停。
三条在两个不同机制（弹性画布 / block dLLM）× 三次实际事故中分别被验证为必要。

## 3. 实验

### 3.1 主表（Seed-TTS testset，转换 ckpt-50000，steps/block=16，CFG 2.0，fp16）

| | SIM-o | WER% | 官方双向基线 SIM/WER |
|---|---|---|---|
| zh | 0.693 | 4.16 | 0.778 / 0.89 |
| en | 0.641 | 3.68 | 0.741 / 1.65 |

解读框架：官方 = 58 万小时**原生形态**；我们 = 100 GPU·时**转换形态**+流式约束
（无右侧上下文、无全局精修）。差距（SIM −0.085~−0.10、WER 2.2~4.7×）
= 流式的质量成本上界（未穷尽推理优化）。

### 3.2 推理刀口消融（zh 304 条子集，锚=steps16/gs2.0/fp16：SIM 0.713/WER 3.45）

| 刀口 | SIM | WER% | 判决 |
|---|---|---|---|
| steps/block 16→32 | 0.713 | 3.79 | 空刀：块内去掩码 16 步已饱和 |
| CFG 2.0→3.0 | 0.710 | **2.89** | **免费午餐：WER −16%，SIM 不掉** |
| CFG 2.0→1.0 | 0.695 | 7.50 | 减弱两头输 |
| CFG 关（gs0） | 0.594 | 25.80 | CFG 完全承重 |
| fp16→fp32 | 0.705 | 3.90 | 平刀：精度无罪 |

CFG 钉点：曲线 0→25.8%（崩）/1.0→7.50/2.0→3.45/2.5→3.22/3.0→2.89/3.5→3.55/4.0→3.07；
3.0 后 WER 噪声平台、SIM 单调付费（0.710→0.683）。**全集复核 gs3.0**：zh 0.680/3.67、
en 0.631/3.59——zh WER −12% 但 SIM −0.013，en 白拧。**主表维持 gs2.0**（子集甜头
全集缩水；方法论教训：子集定方向、全集定去留）。
en 全集 steps32 复核：SIM 0.640（不变）/ WER 6.03（vs 3.68，**+64% 恶化**）——
steps 刀实为反刀：中位帧/EOS率/截断率均不变，恶化是正常生成内的发音退化
（每步解 1 帧 vs 2 帧，重掩码轮次翻倍改写早期正确承诺）。16 步是甜点非妥协。

### 3.3 SIM 缺口归因（三案并审，全部结案）
- 漂移✗：SIM 随生成块数**上升**（zh +0.10 / en +0.30 spearman）。
- 坏尾✗：WER=0 的完美转写句平均 SIM 仍只有 0.708（基线 0.778），缺口弥漫。
- 推理侧✗：steps×2 / CFG 扫描 / fp32 对 SIM 全部无感（3.2 表）。
- **结论：SIM −0.07~−0.10 = 转换训练上限**，即当前配方下"流式的质量成本"的
  训练侧部分。候选杠杆（均需重训）：更长转换、prompt_ratio 配方、数据质量
  子集、向双向老师蒸馏。

### 3.4 口癖之谜：junction EOS 位移（含"数据病"证伪的双否定消融）
- 表型：zh 克隆续写句首语气词率 官方 0.6% vs B2 35.6%（ASR 下限）；zh 插入 96% 句首、
  en 插入 88% 句尾。初版归因为 YODAS 切段口语病（"数据病"假说）。
- **消融证伪（B2F-50k）**：文本级滤除全部边缘 filler 样本后同配方重转换 50k 步，
  生成口癖 20.0% vs 基线 18.2%——**删光数据里的 filler，生成不降**。数据病假说死亡；
  当初 0.6% vs 35.6% 是模型+推理路径的双变量混杂统计。
- **真因（机理链，逐环单变量实证）**：训练把整段尾部掩码区标注为 EOS（~32 列），
  教出"[内容|掩码后缀]=终止区"先验；克隆续写的 junction（[完整句音频|更多文本|继续]）
  恰为该块型 → 概率堆向 EOS（不设地板时 3/4 瞬停 = 当年 30.6% 截断的原病）；
  min_gen_frames 禁掉 EOS 后剩余分布近平 → 确定性 argmax 平局仲裁固定落到 token 0
  （真实数据 rank 275 / 0.134%，且非静音——真静音 cb0=244）；残差码本照常提交
  语音质 token → 声码器渲染为带说话人音色的浊哼鸣。**"emm" = 被禁 EOS 的位移**。
- 佐证：口癖率随 junction 到块边界距离 R 强变（R∈[0,16) 40% vs R≥28 14.1%——
  数据习惯无从感知 R，网格几何可以）；干净锚 prompt 7.7% vs 测试集风格 35.6%（风格门控）；
  CFG 单调压制（先验-证据竞争方向正确）；官方双向基座裁剪前同样有 0.38–1.08s 起始停顿
  （基座天性），转换的净增量 = 停顿升级为 EOS 终止 + 禁令下的哼鸣渲染；
  7 倍放大：数据 ~5% 边缘口癖 → 生成 35.6%（首块先验主导）。
- 排除链（各有对照实验）：声码器冷启动✗（A/B 解码全同）、KV cache 写错✗（fp32 逐位对齐）、
  数据教坏✗（B2F 消融）、数据起始静音模板✗（训练数据 lead-zero 中位数=0）、
  prompt 尾静音✗（0/12）、可见性几何 OOD✗。
- 修复：v1 = EOS 点窗标注（32→4 列）+ prompt_ratio 上限 0.3→0.7 + 推理文本标点对等，
  B2F 三点轨迹（5k/10k/15k）瞬停恒 2/4 → **必要不充分**；v2 = 句边界吸附
  （训练 prompt 切点以 0.5 概率吸附到句末时间戳，补齐 junction 几何），
  判决 = B2G 轨迹：10k 中期读数 瞬停 3/4→2/4、解禁即开火 2-3/4→0-1/4，但表型≈v1 平台；15k 扩样（N=12）终判。[待更新：B2G-15k 判决]
- 护栏规格缺陷（记录在案）：min_gen_frames 仅覆盖首个生成块，越块后禁令静默失效。
- 方法论教训：表型统计不足以归因（0.6% vs 35.6% 曾指向完全错误的病灶）；
  定罪必须机理实验单变量收口——本节的每个环节各有一个专用对照。

### 3.5 能力置换而非叠加
转换后权重在官方定长模式 CER 0.44 → 单套权重不能双模式通吃；
流式形态是"换来的"，不是"加上的"。

### 3.6 分布上限（加训不愈，40k→50k 红格不动）
- 短文本+长音色锚：prompt_ratio≤0.3 训练上限 → 分布外崩（CER 0.75）。
- >10s 超长：max_sample_tokens=2000 上限 → EOS 不触发（0/10）。
归因训练配方而非机制；修复路径 = 配方改动重训（未做，成本考量）。

## 4. Serving 含义 [待填：RTF/首块延迟表]
- 首块延迟 = text prefill + 1 块 × steps 次前向（vs 官方全画布 × steps）。
- [待填] H100 实测 RTF、首块 ms、cache 增量成本曲线。

## 5. 局限与后续
- 定长 32 帧块是折衷；B3 提案：[boundary] token 动态块边界（标点/VAD 停顿对齐）。
- 情感/指令通道在转换中丢失（instruct 实测已死）→ E2-emotion 路线。
- 主表差距的训练侧修复（配方重训）未穷尽。
